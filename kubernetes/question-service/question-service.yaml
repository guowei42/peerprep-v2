apiVersion: v1
kind: Service
metadata:
  name: question-service
  labels:
    app: question-service
spec:
  selector:
    app: question-service
  type: ClusterIP
  ports:
    - name: qn-service
      port: 3002
      targetPort: 3002